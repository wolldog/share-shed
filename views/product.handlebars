<section>
  <img src="/images/{{product.filename}}" alt="{{product.product_name}}">
  <div class="product-details">
    <h3>{{product.product_name}}</h3>
    <h4>{{product.product_model}}</h4>
    <h5>{{product.daily_rate}}</h5>
    <p>{{product.description}}</p>
  </div>
      
        <!-- Display product details -->
        <div class="row w-100">
          <div class="col-md-6">
             <!-- Display product details -->
      <img src="/images/{{product.filename}}" alt="{{product.product_name}}" id="product-image">
      
          </div>
          <div class="col-md-6">
            <h2 id="product-title"></h2>
      <p id="product-description"></p>
            
              <form class="w-100">
                <div class="mb-3">
                  <label for="date" class="form-label">Select date:</label>
                  <input type="date" class="form-control" id="date" name="date">
                </div>
                <a href="">

                  <button id={{product.id}}class="btn btn-primary">Book now</button>
                </a>
              </form>
          </div>
         
        </div>
</section>